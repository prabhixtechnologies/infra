# Company app store — HTML from app-store, APK bytes streamed from S3 by that service.
#
# Enable only after store.prabhixtechnologies.com resolves here. Rollback: rename to .example
# and recreate caddy.

store.prabhixtechnologies.com {
	import security_headers
	reverse_proxy app-store:80
}
