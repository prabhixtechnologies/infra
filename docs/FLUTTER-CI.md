# Copy or symlink this workflow into a git remote that owns `Mobile/`
# (standalone Mobile repo, or a monorepo root that includes Mobile/).

# See Mobile/.github/workflows/flutter-mobile.yml for the full CI definition.
